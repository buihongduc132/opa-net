# Explore Turn 1 — disk-pressure-investigation + ban directive

- Date: 2026-09-09
- Phase: explore
- Status: captured

> **[user]** (verbatim — trust chain L1)
>
> --- ``` $ echo ===== docker root dir =====; docker info 2>/dev/null | grep -iE Docker Root|Storage Driver|Data Space ; echo ---; sudo -n du -sh /var/lib/docker 2>/dev/null || du -sh /var/lib/docker 2>&1; echo --- docker dir listing ---; ls -la /var/lib/docker 2>&1 | head ... (6 earlier lines, ctrl+o to expand) Docker Root Dir: /mnt/sata-docker/docker --- 50M /var/lib/docker --- docker dir listing --- ls: cannot open directory /var/lib/docker: Permission denied $ echo ===== biggest dirs on / =====; sudo -n du -xh -d1 / 2>/dev/null | sort -rh | head -25 || du -xh -d1 / 2>/dev/null | sort -rh | head -25 ... (3 earlier lines, ctrl+o to expand) ===== biggest dirs on / ===== Command timed out after 300 seconds Docker root dir is /mnt/sata-docker/docker (separate SATA mount). So docker images (68GB) live on the SATA disk, NOT on the NVMe /. But the / is 99% full (875G/891G). So the disk pressure is NOT docker images — its something else on /.
>
>  Let me find whats filling / more carefully, with a bounded du. $ echo ===== mounts =====; df -h | grep -vE tmpfs|udev|loop ; echo ===== / du (top level, fast) =====; sudo -n du -xh -d1 / 2>/dev/null | sort -rh | head -20 & sleep 120; kill %1 2>/dev/null; wait 2>/dev/null ... (7 earlier lines, ctrl+o to expand) efivarfs 256K 130K 122K 52% /sys/firmware/efi/efivars /dev/nvme0n1p1 2.0G 11M 2.0G 1% /boot/efi /dev/sdc1 164G 93G 63G 60% /mnt/sata-docker cockpit-tools.AppImage 118M 118M 0 100% /tmp/.mount_cockpiiIlNck ===== / du (top level, fast) ===== $ for d in /home /root /var /usr /opt /mnt /srv /tmp /snap /home/bhd; do printf %-14s  $d; sudo -n du -sh $d 2>/dev/null || echo ?; done ... (2 earlier lines, ctrl+o to expand) ──────────────────────────────────────────────────────────── /home Command timed out after 300 seconds $ echo === /home ===; sudo -n du -sh /home 2>/dev/null; echo === /home/bhd ===; sudo -n du -sh /home/bhd 2>/dev/null; echo === /root ===; sudo -n du -sh /root 2>/dev/null; echo === /var ===; sudo -n du -sh /var 2>/dev/null; echo === /usr ===; sudo -n du -sh /usr 2>/dev/null; echo === /opt ===; sudo -n du -sh /opt 2>/dev/null ``` --- ban all kind of grep / find or any HEAVY cmd from root / and /<1lvl>/<2lvl>; Only be able to these kind of heavy cmd in level 3 and below. Also , make the message to instruct the agent to use eza to find out the directory first before actually doing the recursive search ; LIST all the cmd / tooling in the current machine , all of the one that could do heavy / saturate disk IO / cpu / ram must be BANNED;

Status at end of turn 1: disk pressure on `/` (99% full), heavy scans hang at 300s. User directive: ban heavy cmds at depth ≤2, allow depth ≥3, use eza first, list + ban all IO/CPU/RAM-saturating tools. → Next: turn 2.
